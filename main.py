"""Точка входа приложения ИС учёта успеваемости студентов."""

import os
import sys


def main() -> None:
    """Запускает главное окно приложения.

    Устанавливает рабочую директорию в папку проекта, чтобы
    пути к данным разрешались корректно независимо от того,
    откуда запускается скрипт.
    """
    # Устанавливаем рабочую директорию в папку проекта
    os.chdir(os.path.dirname(os.path.abspath(__file__)))

    from ui.app import App

    app = App()
    app.mainloop()


if __name__ == "__main__":
    main()
